# EduCore Mobile

Expo Router app for the mobile client.

## Development

```bash
npm install
npm run start
```

Useful commands:

- `npm run android`
- `npm run ios`
- `npm run web`
- `npm run lint`

The starter template routes and demo content have been removed. Build the app from the files under `app/`.
